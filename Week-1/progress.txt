Day-1:
- Learned printf(), sizeof()
- Learned variable types, size, declaration

Day-2:
- Learned scanf(), fgets(), getchar(), puchar()
- Learned how stdin/stdout buffering works (covered '\0', '\n', EOF in stdout)
- Wrote stdin cleaner with EOF handler for scanf() and fgets()
- Learned "for", "while" cycles
- Learned if operator
- Learned string principe
- Worked with list (chars array)

Day-3:
- Wrote isDigit(), isAlpha(), printASCIITable() functions. Functional based on ASCI
- Wrote a function which reads user input, saves input number to long long int and checks whether number can be divided by 3 OR 5 (not both). Function works with negative numbers.
- Learned ternary operator
- Wrote function which checks whether input number is a prime number
- Learned sscanf() function
- Wrote a function that reads user input, checks if the entered data is a double, stores it in an array, parses it using sscanf, and adds the number to the total sum

Day-4:
- Learned #define
- Learned escape sequences
- Learned srand(), rand(), time()
- Wrote a function which generates 100 random integers in range [10, 110] and calculates sum of even numbers
- Wrote a function which generates random numbers in range [1, 50] until 25 is generated and counts sum of generated numbers
- Wrote a number guessing game which generates a random number and asks user to guess it with hints
- Wrote a function which generates 20 random numbers in range [-50, 50] and outputs & calculates sum of positive numbers divisible by 3
- Wrote a function which fills an array with random numbers, finds the minimum and maximum values, and calculates the total sum

Day-5:
- Learned switch/case
- Learned struct
- Learned functions types, variable scopes
- Wrote simple cli calculator
- Wrote a function that takes a buffer and its size, writes input from stdin into the buffer, counts the number of spaces and characters, and returns the results in a struct variable