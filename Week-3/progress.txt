Day-13
- Learned FILE *, fopen(), fclose(), fscanf(), fputs(), fread(), fwrite()
- Wrote a function that prints file's content to stdout using fgetc() and putchar()
- Wrote a function that creates a file and writes there some data using fprintf()
- Wrote a function that reads file, saves it's content into buffer and prints it
- Wrote a function that prints file's content to stdout using fgets()
- Wrote a function that writes some string into file and prints it
- Wrote a function that creates array of structs with students data and write them into a binary file
- Wrote a function that reads a binary file with array of structs with students data

Day-14
- Learned fseek(), ftell(), Makefile
- Wrote a function that operates with fseek() and ftell()
- Wrote a Makefile
- Wrote a function that parses and checks arg parameters, saves name and age to allocated memory and prints stats

Day-15
- Learned gdb, valgrid and gcc with -fsanitize
- Learned open(), read(), write(), unlink(), fsync(), lseek()
- Wrote a file that implements use after free, out of bounds and memory leak CWEs
- Wrote a function that creates a file descriptor with open(), unlinks the file, and performs I/O using write() and read()

Day-16
- Learned memfd_create()
- Wrote a function that creates a file with memfd and performs I/O with write() and read()

Day-17
- Practiced working with hexadecimal values
- Learned about fixed-size integers (intX_t)
- Learned basic bitwise operations: AND, OR, XOR, NOT, and left/right shifts
- Wrote a function that demonstrates each bitwise operator
- Wrote a function that encrypts input data using xor and writes it to heap
- Wrote a program that reads input from stdin, encrypts it using my XOR function, and writes encrypted data to memfd. Then reads it and decrypts original contents
- Solved two easy LeetCode problems (arrays)
// It was productive and interesting day